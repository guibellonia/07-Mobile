import React from 'react';
import { View, Text } from 'react-native';
import CustomModalScreen from '../../components/CustomModalScreen';

export default function Slide() {
    return (
        <CustomModalScreen animation='slide' themeColor="#00f" />
    )
}